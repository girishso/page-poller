= simple_form_for(@scraper) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :url
    = f.input :schedule
    = f.input :extract, as: :text
    =# f.association :user

  .form-actions
    = f.button :submit
